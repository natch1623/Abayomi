node_modules/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/

# Las fotos personales de la galería de Abayomi no se publican: la app las lee
# de esta carpeta en local, pero fuera de aquí solo queda la carta de esa
# sección. Si clonas el repo, deja tus imágenes en Fotos/ y aparecerán solas.
/Fotos/
